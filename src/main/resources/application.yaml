spring:
  security:
    user:
      name: admin
      password: admin123
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: admin123
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /locations/h2

app:
  dummy-username: mikica_pikica
