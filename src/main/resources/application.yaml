server:
  address: localhost # 192.168.0.108
  port: 8080 # 80
  ssl:
    enabled: false
    key-store:
    key-store-password:
    key-store-type:
    key-alias:
    key-password:
spring:
  security:
    user:
      name: admin
      password: admin123
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: admin123
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /locations/h2
      settings:
        web-allow-others: true

app:
  dummy-username: "🌼flower_girl🌷"
